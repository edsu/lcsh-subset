Sometimes it might be handy to have a working subset of LCSH for some
applications. This command line program uses the OpenSearch API at
id.loc.gov to do a query, page through the results and build a graph
of the SKOS concepts.
